# Notes: Functions group code into a reusable blocks
# Functions run only when called
# Parameters recieve values when the function is called

def greet(name):
    print(f"Good afternoon, {name}!")

greet("Madison")
greet("Jemel") 